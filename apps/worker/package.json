{
  "name": "@monkey-type/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types",
    "clean": "rm -rf dist .wrangler"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.84.1"
  },
  "dependencies": {
    "@monkey-type/shared": "workspace:*"
  }
}
